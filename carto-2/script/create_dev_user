#!/bin/sh

SUBDOMAIN="development"
PASSWORD="no_more"
ADMIN_PASSWORD="no_more"
EMAIL="sabalah21gmail.com"

echo "--- Creating dev user"
bundle exec rake cartodb:db:create_dev_user SUBDOMAIN="${SUBDOMAIN}" \
        PASSWORD="${PASSWORD}" \
        EMAIL="${EMAIL}"
